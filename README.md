# 1. Use npm install and make sure axios is installed in your system
# 2. Then configure the package.json
# 3. Lastly run the api using npm run dev and should display Sever running on port 3000
# 4. Test the port in localhost:3000/api/optimize/<depot-number>
